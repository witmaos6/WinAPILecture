#pragma once

#include <Windows.h>
#include <iostream>

#include <vector>
using std::vector;

#include <list>
using std::list; // UI 3강에서 queue의 역할을 위해 사용하는데 c++에 이미 큐가 구현되어 있긴 하다.

#include <map>
using std::map;

#include <string>
using std::string;
using std::wstring;

#include <cmath>
#include <cassert>

#pragma comment(lib, "Msimg32.lib")

#include "Define.h"
#include "Func.h"
#include "Struct.h"
